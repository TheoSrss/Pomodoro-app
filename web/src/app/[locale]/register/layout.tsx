

export const metadata = {
    title: "Pomodoro - Register",
};

export default function General({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <div>
            {children}
        </div>
    )
};
