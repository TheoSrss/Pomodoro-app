

export const metadata = {
    title: "Pomodoro - Login",
};

export default function General({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <div>
            {children}
        </div>
    )
};